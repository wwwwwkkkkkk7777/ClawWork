{
  "name": "@clawwork/task-router",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run src/router.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
